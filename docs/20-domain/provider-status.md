# Provider Status

<!-- Generated by go run ./tools/providerstatus -write-doc. Do not edit by hand. -->

> Riido task: RIID-4671

Executable SSOT: [`provider-status.riido.json`](provider-status.riido.json).

`internal/riidoaiserver` owns provider status sync/read and store-safe routing guards.

## Surfaces

| Name | Role |
| --- | --- |
| `ProviderStatusRecord` | provider kind plus routing status row |
| `ProviderStatusSyncRequest` | daemon sync input |
| `ProviderStatusSyncResponse` | server read/write projection |
| `ProviderStatusStore` | provider status writer port |
| `ProviderStatusReader` | provider status reader port |
| `StoreSafeRoutingInput` | assignment routing guard input |
| `StoreSafeRoutingDecision` | assignment routing guard decision |

## Shared Vocabulary

- routing statuses: `available`, `login-required`, `unsupported`, `store-blocked`
- distribution channels: `developer-id`, `mac-app-store`, `msix-sideload`, `msix-store`, `dev-local`

## Sync Validation

- verified rules: `agent-id-required`, `identity-fields-trimmed`, `daemon-id-required`, `runtime-id-required`, `distribution-channel-known`, `providers-required`, `provider-kind-required`, `provider-kind-unique`, `routing-status-known`, `providers-sorted`

## Store-Safe Routing

- verified rules: `runtime-provider-required`, `available-allowed`, `login-required-blocked`, `unsupported-blocked`, `store-blocked-blocked`, `missing-synced-provider-blocked`, `not-synced-allowed`, `unknown-routing-status-rejected`

## Authorization

| Action | Scope |
| --- | --- |
| `write` | `agent:{agent_id}:provider-status:write` |
| `read` | `agent:{agent_id}:provider-status:read` |

## Evidence Loop

| Step | Statement |
| --- | --- |
| Observe | Provider status rules were prose with tests, but the doc itself did not emit evidence or bind shared vocabulary to source anchors. |
| Hypothesis | A provider-status SSOT gate can catch drift in sync validation, routing decisions, authorization scopes, and private-field exclusion before daemon/provider behavior regresses. |
| Execute | Generate this reader doc from the manifest and run the providerstatus verifier in CI. |
| Evaluate | The verifier checks required surfaces, shared vocabulary, sync/routing rules, authorization scopes, source anchors, generated doc freshness, and CLI evidence output. |
| Retrospective | This slice proves server-facing provider status semantics; customer-PC detection, AWS persistence, and deployment evidence remain out of scope. |

## Non-Goals

- `customer-PC provider process execution`
- `executable path detection`
- `login detection`
- `provider provenance collection`
- `store-channel policy decisions`
- `Terraform`
- `AWS`
- `deployment state`
- `raw credentials`
