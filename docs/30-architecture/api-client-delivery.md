# API Client Delivery

<!-- Generated by tools/apiclientdelivery; edit api-client-delivery.riido.json instead. -->

> Riido task: RIID-4746

Executable SSOT: [`api-client-delivery.riido.json`](api-client-delivery.riido.json).

This reader is generated from the generated-client delivery manifest and source evidence.

## Coverage

Source manifests: `4`; owners: `3`; Figma contexts: `9`; source checks: `7`; phrase checks: `29`; forbidden checks: `3`; risk tests: `14`.

## Source Manifests

- AI Agent client API mirror: `docs/20-domain/ai-agent-client-api.riido.json`
- Figma generated-client projection: `docs/30-architecture/figma-ai-agent-control-plane-projection.riido.json`
- runtime CD public surface scan: `docs/30-architecture/runtime-cd-ownership.riido.json`
- AI Agent risk evidence: `docs/30-architecture/ai-agent-risk-evidence.riido.json`

## Ownership

- `riido-contracts` owns canonical vocabulary, domain policy grammar, lifecycle/deprecation grammar; does not own control-plane handler behavior or client branches.
- `riido-control-plane` owns AI Agent client API sub-DSL, HTTP/SSE implementation, OpenAPI projection handoff, generated-client delivery workflow, release manifest; does not own canonical business meaning outside API projection.
- `riido-client` owns consuming generated React Query code and API usability feedback; does not own OpenAPI SSOT, control-plane codegen, generated-code hand edits.

## Release Trigger And Branch

- workflow: `.github/workflows/generated-client-delivery.yml`
- package mode: create_pr=false builds the review artifact without requiring `riido-client` write credentials
- delivery mode: create_pr=true opens or updates a riido-client generated handoff PR
- missing credential behavior: This failure is intentional only for `create_pr=true` when cross-repository credentials are absent.
- branch source: Riido `branchName`
- branch rule: target_branch must match ^[A-Z][A-Z0-9]*-[0-9]+-.+ and must not contain path separators
- branch example: `A-60-AI-Agent-generated-client-handoff`
- guard: delivery requires branch-name and secret gates before cross-repository write

## Generator Boundary

- React Query generator: `tools/reactquerygen`
- handoff generator: `tools/generatedclienthandoff`
- artifacts: `aiAgentClient.ts`, `aiAgentClient.react.ts`, `README.generated.md`, `contractManifest.generated.ts`, `HISTORY.generated.md`, `index.ts`, `PR_BODY.generated.md`
- must not own: riido-client application implementation; frontend route layout; manual generated-code edits

## Figma Consumption Contexts

### menu-placement

- node ids: `156-19307`
- generated paths: `none`
- rule: menu placement is generated-client consumption context, not endpoint evidence
- must not own: client menu labels, ordering, selected state, or route wiring

### task-thread

- node ids: `153-15931`, `236-21379`
- generated paths: `riido.aiAgent.tasks.assign`, `riido.aiAgent.tasks.unassign`, `riido.aiAgent.tasks.stop`, `riido.aiAgent.tasks.threads`, `riido.aiAgent.events.stream`
- rule: active_stream is the generated handoff into SSE when present
- must not own: comment box, row layout, right panel, or stopped-row presentation

### busy-agent

- node ids: `153-8761`
- generated paths: `riido.aiAgent.tasks.threadMessages.create`, `riido.aiAgent.tasks.submitComment`, `riido.aiAgent.tasks.stop`
- rule: assignment_ready is daemon/runtime handoff and must not be rendered as queued_by_busy_agent
- must not own: Korean display copy, timestamp wording, avatar, or comment-row layout

### deleted-agent-stop

- node ids: `227-19354`
- generated paths: `riido.aiAgent.agents.delete`, `riido.aiAgent.tasks.threads`, `riido.aiAgent.events.stream`
- rule: delete can expose running_tasks_force_stopped and stopped_by_agent_deleted
- must not own: stopped-row copy, actor label, timestamp wording, hidden action state, avatar, or row layout

### participant-dropdown

- node ids: `153-12742`
- generated paths: `listAIAgentTaskAssignableAgents`
- rule: query feeds AI Agent rows in the participant dropdown
- must not own: sorting, truncation, max-height, scrollbar width, or checkbox layout

### runtime-settings

- node ids: `162-23090`
- generated paths: `listAIAgentDeviceRuntimes`, `getAIAgentDeviceDaemon`, `startAIAgentDeviceDaemon`, `restartAIAgentDeviceDaemon`, `stopAIAgentDeviceDaemon`
- rule: generated pieces feed device/runtime grouping and daemon controls
- must not own: hover popover, stop modal layout, or restart animation rendering

### agent-settings

- node ids: `432-37336`, `134-6542`, `432-35713`
- generated paths: `createAIAgent`, `updateAIAgent`, `deleteAIAgent`
- rule: created_at and updated_at feed list dates and tooltips
- must not own: row click, meatball menu, status label copy/color, save enablement, or provider-specific model labels

### onboarding

- node ids: `42-3014`
- generated paths: `riido.aiAgent.onboarding.fixtures`, `riido.aiAgent.onboarding.fixtures.createAgent`
- rule: fixtures create a normal agent with CreateAgentConfigurationRequest
- must not own: workspace selector layout, copied fixture text as SSOT, or app-start helper endpoints

### web-onboarding

- node ids: `236-29749`
- generated paths: `none`
- rule: auth/onboarding screens are not generated-client endpoint evidence for this API
- must not own: sign-up/login, terms consent, invite, app download, waitlist, or marketing-consent helpers

## Runtime Model Catalog

- policy: `runtime_model_catalog.v1`
- rendering: clients render `RuntimeRecord.models`; model labels stay runtime-scoped display data rather than enum members
- fixture rule: Fixture records must not include model defaults or `model_id`; clients rely on the omitted-`model_id` defaulting rule.

## AI Agent Risk Evidence

- `private-worktree-url-redaction`: `TestStoreActorDropsSensitiveAssignmentWorktreeURL` proves Assignment worktree URLs are dropped before durable store persistence.
- `active-stream-handoff`: `TestHTTPAIAgentClientDevelopmentV2WorkspaceScopedCreateAndThreadStream` proves Task action responses expose the active stream handoff for queued or running work.
- `terminal-late-progress-fence`: `TestC2LateRuntimeProgressDoesNotReactivateStoppedThread` proves Late daemon progress cannot reactivate a stopped terminal task thread.
- `assign-reconcile-scope`: `TestHTTPAIAgentClientAssignReconcilesRequestedTaskOnly` proves Assign mutation reconcile scope stays on the requested task instead of scanning unrelated active work.
- `stale-terminal-busy-count`: `TestAIAgentClientStaleTerminalAgentCountDoesNotQueueNextAssignment` proves Terminal stale projections do not make an agent look busy for new assignment decisions.
- `partial-progress-coalescing`: `TestDevelopmentAIAgentClientStoreCoalescesAssistantPartialThreadProgressBatches` proves Assistant partial progress is coalesced before read-model fanout.
- `projection-read-repair`: `TestHTTPTaskThreadListRepairsStaleReadModelFromAssignmentProjection` proves Cold task-thread reads can repair stale read models from durable assignment projection.
- `additive-agent-active`: `TestStoreActorAdditiveAssignmentKeepsExistingAgentActive` proves Adding another agent keeps existing active agent assignment state intact.
- `generated-fsm-control-plane-consumption`: `TestTaskFSMBaseline` proves Control-plane consumes contracts generated task FSM service provider.
- `generated-fsm-control-plane-consumption`: `TestAssignmentFSMBaseline` proves Control-plane consumes contracts generated assignment FSM service provider.
- `web-approval-contract-consumption`: `TestAssignmentApprovalBaseline` proves Control-plane consumes contracts tool approval timeout status and decision vocabulary.
- `web-approval-round-trip`: `TestHTTPToolApprovalRoundTrip` proves Daemon-created tool approvals can be listed and decided through client v1/v2 routes, and daemon wait is released by the web decision.
- `generated-fsm-conformance`: `TestVerifyGeneratedFSMFiles` proves Contracts fsmgen verifies generated FSM drift and conformance from Common Lisp metadata.
- `web-approval-contract`: `TestAssignmentContractToolApprovalWireShape` proves Contracts owns the tool approval request/decision/result DTO wire shape.

## Lifecycle States

- `active`
- `deprecated`
- `superseded`
- `frozen`
- `removed_from_active_projection`
- `archived`

## Non-Goals

- `riido-client application implementation`
- `canonical contract changes`
- `Figma coverage redefinition`
- `live delivery execution`
- `automatic riido-client PR merge`

## Evidence Loop

| Step | Evidence |
| --- | --- |
| Observe | The API client delivery document repeated generated-client workflow, branch, Figma context, model-catalog, and permission rules as prose while executable generators and manifests already existed. |
| Hypothesis | A delivery manifest can generate the reader and verify each frontend handoff claim against workflow, generator, projection, risk, and public-surface evidence. |
| Execute | Generate this reader from the delivery manifest and run the apiclientdelivery verifier in CI. |
| Evaluate | The verifier checks source anchors, required reader phrases, forbidden stale phrases, linked source manifests, workflow token gates, and generated doc freshness. |
| Retrospective | This keeps generated-client delivery reviewable without making the prose document a second source of truth. |
